FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml ./
RUN pip install --no-cache-dir uv && uv sync --no-dev
COPY src ./src
COPY configs ./configs
ENV PYTHONPATH=/app
CMD ["uv", "run", "uvicorn", "src.serve.app:app", "--host", "0.0.0.0", "--port", "8000"]
